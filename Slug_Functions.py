#Our functions, first function calls next function and the results
#fall like a domino stack.
from GLOB_VAR import zipcode

def menuloop():
    #usual while, if, loop that error checks and sends the input to the next function
    return

def zipcode_Find(): #menuloop user entry goes into zipcode find.
    #zipcode find sends a get request to the server
    #zipcode find also checks to make sure that we have a proper connection
    #zipcode find messages user to try again later, erases zipcode and dict entry
    #sends user back to menuloop at the beginning.
    #may make that a seperate function later on.
    return

def question_loop():
    #similar to menu loop, just asks the user if they're interested in trying again
    return
